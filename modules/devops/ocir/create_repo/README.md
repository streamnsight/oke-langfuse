# OCIR Create Repo Module

This module provisions a container image repository in Oracle Cloud Infrastructure Registry (OCIR). It ensures the repository exists for storing and managing Docker container images.
