# OCIR Push Helm Chart Module

This module handles pushing Helm chart artifacts to Oracle Cloud Infrastructure Registry (OCIR). It automates the upload process for deploying Helm charts in OCI environments.
