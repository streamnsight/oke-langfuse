# OCIR Push Image Module

This module handles pushing Docker or OCI container images to Oracle Cloud Infrastructure Registry (OCIR). It automates the image upload process required for container deployments in OCI.
