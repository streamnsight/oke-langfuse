# Cert-Manager Add-on Module

This module provisions and configures the cert-manager add-on for Oracle Kubernetes Engine (OKE) clusters. It automates management of TLS certificates and certificate issuers in the cluster.
