# Native Ingress Add-on Module

This module provisions and manages the native ingress add-on for Oracle Kubernetes Engine (OKE) clusters. It configures ingress resources for routing external traffic to services running in the cluster.
