# IDCS Application Module

This module provisions and manages an Oracle Identity Cloud Service (IDCS) application. It supports integration of OCI workloads with IDCS for authentication, authorization, and identity management.
