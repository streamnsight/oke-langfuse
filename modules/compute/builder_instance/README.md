# Builder Instance Module

This module provisions a builder VM instance intended for use in CI/CD pipelines or automated build processes. It configures the necessary compute, network, and provider resources for a dedicated build server.
