# Shape Availability Module

This module queries available compute shapes (instance types) in the target region or availability domain. It helps determine what compute resources can be provisioned and enables dynamic instance type selection.
